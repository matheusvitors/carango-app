const prefix = '@carango'

export const KEY = {
	THEME: `${prefix}:theme`,
	TOKEN: `${prefix}:token`,
	REFRESH_TOKEN: `${prefix}:refresh-token`,
	CREDENTIALS: `${prefix}:credentials`,
	SELECTED_CAR: `${prefix}:selected-car`
}
